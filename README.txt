Initial Password is "admin".
Save the password file in the same folder in which you will execute the JAVA file.
To connect JAVA to ORACLE DATABASE you require the ojdbc14.jar file(in the same file) and also the oracle database software.
first you need to create table items in the database(further information can be gathered by commented lines of code).